# {{serviceName}} (BFF)

Backend-for-Frontend scaffold gerado pelo `scaffolding-cli`.

Use este template para endpoints que agregam APIs externas e adaptam para o frontend.

## 🚀 Executar com Docker (Recomendado)

### Pré-requisitos

- Docker instalado
- Docker Compose instalado

### Configuração

1. Copie o arquivo de exemplo de variáveis de ambiente:
```bash
cp .env.example .env
```

2. Edite o `.env` conforme necessário:
```env
NODE_ENV=production
PORT=3000
UPSTREAM_BASE_URL=http://localhost:3001
```

### Subir o serviço

```bash
# Buildar e iniciar o container
docker-compose up -d

# Ver logs
docker-compose logs -f

# Parar o container
docker-compose down
```

O serviço estará disponível em `http://localhost:3000`

### Comandos úteis

```bash
# Rebuild da imagem
docker-compose build

# Reiniciar o serviço
docker-compose restart

# Ver status
docker-compose ps

# Entrar no container
docker-compose exec {{serviceName}} sh
```

## 💻 Executar localmente (sem Docker)

Passos

```bash
npm install
npm run build
npm start
```

## 🏥 Health Check

Health: GET /health

## 📡 Exemplo BFF

O template inclui um cliente HTTP e rota de exemplo `GET /api/external` que chama um upstream via `UPSTREAM_BASE_URL`.

Defina em `.env`:

```
UPSTREAM_BASE_URL=http://localhost:3001
```
