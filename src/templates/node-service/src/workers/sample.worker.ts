// Exemplo de worker que processa mensagens de forma genérica (placeholder)
export class SampleWorker {
  start() {
    // Implementar consumo usando AWS SDK v3 ou biblioteca adequada (SQS/SNS)
    console.log('Worker started (placeholder)');
  }
}
