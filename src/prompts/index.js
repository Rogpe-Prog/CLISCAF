import { serviceNamePrompt } from './service-name.prompt.js';
import { destinationPrompt } from './destination.prompt.js';
import { serviceTypePrompt } from './service-type.prompt.js';

export const initPrompts = [
  serviceNamePrompt,
  serviceTypePrompt,
  destinationPrompt
];

export { serviceNamePrompt, destinationPrompt, serviceTypePrompt };
